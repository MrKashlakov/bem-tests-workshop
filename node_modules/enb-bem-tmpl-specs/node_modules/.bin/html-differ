#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../html-differ/bin/html-differ" "$@"
  ret=$?
else 
  node  "$basedir/../html-differ/bin/html-differ" "$@"
  ret=$?
fi
exit $ret
